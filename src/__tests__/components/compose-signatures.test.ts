import { ComposeModal } from '@/components/email/ComposeModal';

describe('ComposeModal component export', () => {
  it('exports ComposeModal React component', () => {
    expect(typeof ComposeModal).toBe('function');
  });
});
