import { ComposeAiWriter } from '@/components/email/ComposeAiWriter';

describe('ComposeAiWriter component', () => {
  it('exports ComposeAiWriter React component function', () => {
    expect(typeof ComposeAiWriter).toBe('function');
  });
});
