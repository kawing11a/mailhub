import { EmailExplainPanel } from '@/components/email/EmailExplainPanel';

describe('EmailExplainPanel component', () => {
  it('exports EmailExplainPanel React component function', () => {
    expect(typeof EmailExplainPanel).toBe('function');
  });
});
