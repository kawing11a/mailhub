import { SignatureModal } from '@/components/signatures/SignatureModal';

describe('SignatureModal component export', () => {
  it('exports SignatureModal React component', () => {
    expect(typeof SignatureModal).toBe('function');
  });
});
